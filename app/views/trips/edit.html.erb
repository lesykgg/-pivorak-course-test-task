<% if current_user.try(:admin?) %>
<h1>Editing Trip</h1>

<%= render 'form', trip: @trip %>

<%= link_to 'Show', @trip %> |
<%= link_to 'Back', trips_path %>

<% else %>
<strong>Access forbidden</strong>
<% end %>
